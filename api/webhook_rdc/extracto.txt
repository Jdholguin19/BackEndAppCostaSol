POST
Bulk update dataset records
https://subdomain.kissflow.com/dataset/2/:account_id/:dataset_id/batch
This endpoint lets you bulk create/update up to 500 dataset records and values at a time.

HEADERS
Content-Type
multipart/form-data

Accept
application/json

X-Access-Key-Id
(Required) Access key ID

X-Access-Key-Secret
(Required) Access key secret

PATH VARIABLES
account_id
<string>

(Required) Kissflow Account ID. Learn how to retrieve an Account ID from Kissflow.

dataset_id
<string>

(Required) The unique identifier for each dataset. You can find the dataset ID from the web URL. For instance, in the URL, https://thestarkcorporation.kissflow.com/view/dataset/Bank_Dataset, Bank_Dataset will be the dataset ID. Contact customer support if you need further assistance.

Body
formdata
Data
[Object]

Add records to the body payload.

Example Request
200 OK
View More
curl
curl --location 'https://subdomain.kissflow.com/dataset/2/:account_id/:dataset_id/batch' \
--header 'Content-Type: multipart/form-data' \
--header 'Accept: application/json' \
--header 'X-Access-Key-Id: <string>' \
--header 'X-Access-Key-Secret: <string>' \
--form '0="[object Object]"' \
--form '1="[object Object]"'
200 OK
Example Response
Body
Headers (1)
View More
json
[
  {
    "Name": "<string>",
    "_id": "<string>",
    "PONumber": "<string>",
    "TotalPrice": "<string>",
    "RequestedBy": {
      "_id": "<string>",
      "Name": "<string>"
    }
  },
  {
    "Name": "<string>",
    "_id": "<string>",
    "PONumber": "<string>",
    "TotalPrice": "<string>",
    "RequestedBy": {
      "_id": "<string>",
      "Name": "<string>"
    }
  }
]
POST
Bulk update records of a dataset view
https://subdomain.kissflow.com/dataset/2/:account_id/:dataset_id/view/:view_id/batch
This endpoint lets you bulk create/update up to 500 fields and values of a specified dataset view.

HEADERS
Content-Type
multipart/form-data

Accept
application/json

X-Access-Key-Id
<string>

(Required) Access key ID

X-Access-Key-Secret
<string>

(Required) Access key secret

PATH VARIABLES
account_id
<string>

(Required) Kissflow Account ID. Learn how to retrieve an Account ID from Kissflow.

dataset_id
<string>

(Required) The unique identifier for each dataset. You can find the dataset ID from the web URL. For instance, in the URL, https://thestarkcorporation.kissflow.com/view/dataset/Bank_Dataset, Bank_Dataset will be the dataset ID. Contact customer support if you need further assistance.

view_id
<string>

(Required) The unique identifier for each dataset view. You can find the dataset view ID from the web URL. For instance, in the URL, https://thestarkcorporation.kissflow.com/view/dataset/Bank_Dataset/view/Bank_statements, Bank_statements will be the dataset view ID. Contact customer support if you need further assistance.

Body
formdata
Add record
Record

Add a dataform record in the body payload